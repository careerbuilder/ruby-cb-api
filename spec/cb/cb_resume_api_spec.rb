require 'spec_helper'

module Cb
  describe Cb::ResumeApi do
    context '.own_all' do
      #Cb.resume.retrieve
      #resumes = Cb.resume.own_all('XRHP5HT66R55L6RVP6R9', true)

      #expect(resumes.cb_response.errors.nil?).to be true
      #expect(resumes.nil?)
    end
  end
end