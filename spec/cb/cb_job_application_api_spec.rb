require 'spec_helper'

module Cb
  describe Cb::JobApplicationApi do
    context '.get_for job did'

    #TODO
    
    # Some thought needs to be put into how we test this (can't use a DID). Search for jobs until we find one with a "legit" application type???? That feels bad but......
    
    # it 'should get an app ', :vcr => { :cassette_name => 'application/for_job'} do
      # ret = Cb.job_application.get_for("JHL6166Z63N3ZDVZP74")
 
      # puts ret.to_yaml
    # end
  end
end